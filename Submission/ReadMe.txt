1. import every package and java files from the TeamProject.zip file to a new java project.

2. run users sql files on datbase.sql, wheelOfFortune.sql, and testWheelOfFortune.sql in order.

3. run the spinwheelServer.bat file

4. run the spinwheelClients.bat file
	- the first parameter of client is not set to local host, 
	  if the host is local change it to "localhost"
